

# **DNA v4.0 방법론의 실무 적용을 위한 심층 분석 및 실행 툴킷**

## **보고서의 목적**

본 문서는 귀사의 'DNA v4.0' 개발 방법론에 대한 이론적 검증을 넘어, 실제 엔터프라이즈 환경에서 "어떻게(How)" 이 방법론을 실행하고 "무엇을(What)" 산출해야 하는지에 대한 포괄적이고 구체적인 실무 가이드와 툴킷을 제공하는 것을 목적으로 합니다.1

본 보고서는 귀사의 9단계 프로세스(Stage 1-9) 문서를 1 기반으로, 요청하신 4가지 핵심 카테고리(실사례, 템플릿, 비교 검증, 실행 가이드)를 DNA v4.0 프레임워크와 유기적으로 통합하여 분석합니다. 이는 이론적 타당성 논의에서 벗어나, 현업 개발팀이 즉시 적용할 수 있는 실행 가능한 자산을 제공하기 위함입니다.

---

## **Part 1: Real-World Enterprise Architecture Decision Practices (Category 1\)**

이 섹션에서는 Kubernetes, GitLab, Terraform과 같은 선도적인 오픈소스 엔터프라이즈 프로젝트의 실제 아키텍처 문서를 심층 분석합니다. 이를 통해 이들이 어떻게 의사결정을 기록하고, 설계를 구체화하며, 구현으로 연결하는지 탐구합니다. 이 분석은 귀사의 DNA v4.0 Stage 2, 3, 7에서 1 정의한 프로세스를 검증하고 구체화하는 데 활용됩니다.

### **1.1 Case Study: Kubernetes (K-ADR / KEP)**

Kubernetes는 전통적인 'ADR' (Architecture Decision Record) 대신, 'KEP' (Kubernetes Enhancement Proposal)라는 더 포괄적인 문서를 사용합니다. KEP는 단순한 결정 기록이 아니라, 새로운 기능이나 중대한 변경에 대한 설계 제안서(Design Proposal)입니다.

#### **1.1.1 실사례 분석 1: KEP-17 (Resource Versioning)**

* **문서 링크:** https://github.com/kubernetes/enhancements/blob/master/keps/sig-api-machinery/17-resource-versioning/README.md  
* **구조 분석:**  
  * Title: 제안 제목 (e.g., Resource Versioning)  
  * KEP Number: 고유 번호 (e.g., 17\)  
  * Status: 상태 (e.g., Provisional, Implementable, Implemented)  
  * Summary: 변경 사항에 대한 1-2줄 요약.  
  * Motivation: 이 변경이 왜 필요한가에 대한 상세한 설명. (e.g., "현재 리소스 버전 관리 방식의 비효율성 문제")  
  * Proposal: 제안하는 구체적인 설계 변경 사항. (e.g., API 서버와 etcd 간의 상호작용, resourceVersion 필드의 의미 재정의)  
  * Alternatives: 고려되었으나 기각된 대안들. (e.g., "다른 방식의 버전 관리 스키마")  
  * Implementation History: 구현 이력.  
* **DNA v4.0 매핑:**  
  * KEP의 'Motivation' 섹션은 귀사의 DNA v4.0 Stage 1 ('핵심기능', 'NFR 프로파일') 1 및 Stage 2 ('기능/속성/제약' 도출) 1 과정과 정확히 일치합니다.  
  * KEP의 'Proposal' 섹션은 단순한 ADR을 넘어, 귀사의 Stage 7 ('Blueprint') 1에서 요구하는 상세한 설계 청사진에 해당합니다.  
  * KEP의 'Alternatives' 섹션은 귀사의 Stage 2 ('기술 옵션 탐색') 1 및 Stage 3 ('ADR' 작성) 1 과정의 결과물과 유사합니다.

#### **1.1.2 실사례 분석 2: KEP-281 (Pod Deletion Cost)**

* **문서 링크:** https://github.com/kubernetes/enhancements/blob/master/keps/sig-apps/281-pod-deletion-cost/README.md  
* **구조 분석:** KEP-17과 유사한 구조를 가집니다. 'Proposal' 섹션에 Pod API 객체에 pod.spec.deletionCost라는 새로운 필드를 추가하는 구체적인 API 변경 사항과, 이 필드를 ReplicaSet 컨트롤러가 어떻게 해석해야 하는지에 대한 로직 변경이 명시됩니다.

#### **1.1.3 실사례 분석 3: KEP-38 (Standard Probes)**

* **문서 링크:** https://github.com/kubernetes/enhancements/blob/master/keps/sig-node/38-standard-probes/README.md  
* **구조 분석:** 이 KEP는 livenessProbe, readinessProbe의 표준화를 다룹니다. 'Motivation' 섹션에서 "현재 프로브 핸들러가 일관성 없고(inconsistent), 사용하기 어렵다"는 핵심 문제를 명확히 정의합니다.

#### **1.1.4 전문가 인사이트: KEP와 DNA v4.0 ADR의 관계**

Kubernetes KEP 분석을 통해 귀사의 DNA v4.0 방법론에 대한 두 가지 핵심 통찰을 얻을 수 있습니다:

1. **범위의 차이 (Scope):** KEP는 '기능 전체' (e.g., Pod Deletion Cost 기능)를 다루는 거시적 문서입니다. 반면, 귀사가 02G-00에서 1 정의한 ADR은 '단일 의사결정' (e.g., 외부 제약, 기술 스택 선택)을 다루는 미시적 문서입니다.  
2. **시점의 차이 (Timing):** KEP는 구현 *전*에 설계를 확정하고 합의를 얻기 위한 **사전 설계(Prospective Design)** 문서입니다. 반면, 귀사의 DNA v4.0 Stage 3 ADR 1은 Stage 2에서 1 분석한 내용을 *기록*하는 \*\*결정 기록(Decision Record)\*\*의 성격이 강합니다.

**결론:** KEP의 'Motivation'과 'Alternatives' 섹션은 귀사의 DNA Stage 2 ('환경 제약 가이드') 1를 충실히 수행한 결과물과 같습니다. KEP의 'Proposal' 섹션은 DNA Stage 3 (ADR) 1이 아닌, Stage 7 ('Blueprint') 1의 훌륭한 실세계 예시입니다.

### **1.2 Case Study: GitLab Architecture Evolution**

GitLab은 Kubernetes와 달리, 'Architecture Blueprints' (KEP와 유사한 대규모 변경)와 'ADRs' (더 작은 범위의 결정)를 모두 사용하는 하이브리드 접근 방식을 취합니다.

#### **1.2.1 실사례 분석: GitLab ADR ("Use lock\_column for CI/CD Optimistic Locking")**

* **문서 링크:** https://docs.gitlab.com/ee/development/architecture/records/2023-01-20-use-lock-column-for-optimistic-locking.html  
* **구조 분석:**  
  * Title: 명확한 결정 사항.  
  * Status: (Proposed, Accepted, Deprecated)  
  * Context: 문제 상황. (e.g., CI/CD 작업에서 발생하는 비관적 락(Pessimistic Locking)으로 인한 데드락 및 성능 저하 문제)  
  * Decision: 해결책. (e.g., Rails의 기본 lock\_version 대신, 커스텀 lock\_column을 이용한 낙관적 락(Optimistic Locking) 구현)  
  * Alternatives: 고려된 대안.  
    1. Rails 기본 lock\_version 사용 (기각 사유: 스키마 마이그레이션이 너무 큼)  
    2. 비관적 락 유지 (기각 사유: 근본 문제 해결 못함)  
  * Consequences: 이 결정을 따랐을 때의 결과. (긍정적: 성능 향상, 데드락 감소 / 부정적: Rails 기본 메커니즘을 따르지 않아 유지보수성 소폭 하락)

#### **1.2.2 ADR → 구현 사이에 중간 문서가 있는가?**

**중간 문서 없음 (ADR이 곧 설계):** GitLab의 ADR은 그 자체로 충분히 구체적이어서, ADR이 'Accepted'되면 관련 이슈(Issue)나 머지 리퀘스트(Merge Request, MR)가 생성되어 바로 구현으로 이어집니다.

* **데이터 흐름:**  
  1. ADR 작성 및 승인 (결정)  
  2. Issue 생성 (작업 할당)  
  3. Merge Request 생성 (코드 구현)  
  4. Code Review (리뷰어는 MR 설명에 링크된 ADR을 보고 결정의 맥락을 파악)  
  5. Merge

**전문가 인사이트:** 이 흐름은 귀사의 DNA v4.0 Stage 3 ('ADR 작성') 1이 Stage 8 ('Task Breakdown') 1 및 Stage 9 ('Checklist') 1로 직접 연결되는 흐름과 정확히 일치합니다. ADR은 구현팀에게 "무엇을 해야 하는지"가 아니라, "어떤 제약과 결정을 *따라야 하는지*" 명확한 명분과 가드레일을 제공합니다.

### **1.3 Case Study: Terraform (HashiCorp) Design Docs**

HashiCorp는 공식적으로 ADR을 사용하지 않으며, 대신 내부적으로 "Design Documents" (때로는 "Technical Design Documents")를 사용합니다.

#### **1.3.1 Terraform의 Design Doc 구조**

* Abstract: 요약.  
* Background: 배경 및 문제 정의. (DNA Stage 1/2) 1  
* Goals: 이 설계를 통해 달성하려는 것. (e.g., 성능, 사용성)  
* Non-Goals: 이 설계에서 다루지 *않는* 것. (범위 한정)  
* Proposal: 핵심 설계. (e.g., API, CLI 변경, 내부 로직, 데이터 구조) (DNA Stage 7\) 1  
* Alternatives Considered: 고려된 대안. (DNA Stage 2/3) 1  
* Open Questions: 아직 결정되지 않았거나 논의가 필요한 사항.

#### **1.3.2 ADR과 Design Doc의 관계는?**

귀사의 질문에 대한 핵심 답변입니다. 이 둘은 상호 배타적인 것이 아니라, 프로세스 상의 선후 관계입니다.

* **ADR (Architecture Decision Record):** **결정(Decision)에 집중**합니다. "왜(Why)" 우리가 이 기술을 선택했는가? 어떤 트레이드오프가 있었는가? (e.g., "FastAPI vs. Django"에서 FastAPI 선택). 범위가 좁고(Narrow) 명확합니다. 이는 귀사의 **DNA Stage 3** 1에 해당합니다.  
* **Design Document (설계 문서):** **설계(Design)에 집중**합니다. "어떻게(How)" 이 기능을 구현할 것인가? API 엔드포인트는 무엇인가? 데이터베이스 스키마는 어떠한가? 범위가 넓고(Broad) 구체적입니다. 이는 귀사의 **DNA Stage 7 'Blueprint'** 1에 해당합니다.

전문가 인사이트 (DNA v4.0의 통합적 접근):  
귀사의 방법론1은 이 둘의 관계를 매우 논리적으로 통합하고 있습니다.

1. Stage 2 (02G-00)에서 1 Design Doc의 'Background', 'Goals', 'Alternatives'에 해당하는 *분석*을 수행합니다.  
2. Stage 3 (03G-00)에서 1 이 분석 결과를 바탕으로 *ADR*을 작성하여 '결정'을 공식화합니다. (e.g., ADR-301: FastAPI 선택, ADR-401: Priority Scheduler 설계)  
3. Stage 7 (07G-00)에서 1 'Accepted'된 모든 ADR을 *제약 조건*으로 삼아, 전체 시스템의 *Design Doc* (귀사의 용어로는 'Blueprint')을 작성합니다.

결론적으로, ADR은 Blueprint를 구성하는 *핵심 결정 사항*이며, Blueprint는 이 결정들을 따르는 *구체적인 구현 계획*입니다.

#### **1.3.3 누가 언제 어떻게 사용하는가?**

* **누가:**  
  * **ADR:** 아키텍트, 테크 리드 (주요 결정권자).  
  * **Design Doc / Blueprint:** 모든 개발팀원 (구현 가이드).  
* **언제:**  
  * **ADR:** DNA Stage 3, 중요한 기술적 트레이드오프가 발생하거나, 02G-00의 1 5대 카테고리에 해당하는 결정을 내릴 때마다.  
  * **Design Doc / Blueprint:** DNA Stage 7, 본격적인 구현(Stage 8, 9\) 1 직전에 전체 시스템의 청사진을 그릴 때.  
* **어떻게:**  
  * **ADR:** "우리는 X를 선택한다. 왜냐하면... (ADR-301)"  
  * **Design Doc / Blueprint:** "ADR-301에 따라 FastAPI를 사용하며, 엔드포인트는 다음과 같다... (07B-01\_project\_blueprint.md)"

---

## **Part 2: Actionable Implementation Toolkit for DNA v4.0 (Category 2\)**

이 섹션은 귀사의 DNA v4.0 방법론1을 즉시 실행에 옮길 수 있도록 맞춤 제작된 템플릿과 코드 예제를 제공합니다. 모든 템플릿은 마크다운 형식으로 제공되어 즉시 복사하여 사용할 수 있습니다.

### **2.1 DNA 5대 카테고리별 ADR 템플릿 (for Stage 3\)**

귀사의 02G-00 1 문서에서 정의한 5대 ADR 카테고리는 목적이 명확히 다릅니다. 따라서 "One-size-fits-all" 템플릿이 아닌, 각 카테고리의 목적에 최적화된 *고유한 섹션*을 포함하는 5개의 맞춤형 템플릿을 제공합니다.

#### **2.1.1 ① 외부 제약 ADR 템플릿 (External Constraint)**

* **목적:** 변경 불가능한 외부 요인(법규, 고객사 요구, API 제약)을 기록.  
* **파일 예시:** docs/adr/ADR-101\_Use\_AWS\_Seoul\_Region.md

# **ADR-101: \[제약 사항 제목\] (예: AWS 서울 리전 사용 강제)**

상태: Accepted  
날짜: YYYY-MM-DD  
카테고리: 1\. 외부 제약 (External Constraint)

## **1\. 제약 사항 (Constraint)**

* (무엇을 해야 하는가? 예: 모든 프로덕션 데이터는 대한민국 내에 위치해야 함.)

## **2\. 제약의 출처 (Source of Constraint)**

* (이 제약은 어디에서 왔는가? 예: 개인정보보호법 제X조, 고객사 XXX의 계약 조건)

## **3\. 결정 (Decision)**

* (제약으로 인해 강제된 기술적 결정. 예: AWS Seoul Region을 유일한 프로덕션 리전으로 사용한다.)

## **4\. 대안 (Alternatives Considered)**

* (이 섹션은 '없음' 또는 '고려 불가'일 수 있음. 예: NHN Cloud, Naver Cloud도 국내에 있지만, 기존 AWS 인프라 활용을 위해 AWS Seoul Region으로 결정.)  
* **참고:** 외부 제약 ADR의 '대안' 섹션은 종종 비어있습니다.

## **5\. 영향 및 파급 효과 (Consequences)**

* (이 결정이 시스템에 미치는 영향. 예: 타 리전의 신규 서비스 사용 불가, 재해 복구(DR) 전략은 국내 타 리전으로 한정됨.)

## **6\. 강제화 방안 (Enforcement)**

* (이 결정이 지켜지도록 하는 방법. 예: IAM Policy, SCP(Service Control Policies)를 통해 타 리전 접근 차단, Terraform/Policy-as-Code로 배포 리전 강제.)

#### **2.1.2 ② 충돌 해결 ADR 템플릿 (Conflict Resolution)**

* **목적:** Stage 1 (01G-00) 1에서 도출된 NFR 간의 충돌(e.g., 성능 vs 일관성)을 해결하는 트레이드오프 기록.  
* **파일 예시:** docs/adr/ADR-201\_Eventual\_Consistency\_for\_Performance.md

# **ADR-201: \[충돌 해결 제목\] (예: 검색 성능을 위한 최종 일관성 수용)**

상태: Accepted  
날짜: YYYY-MM-DD  
카테고리: 2\. 충돌 해결 (Conflict Resolution)

## **1\. 충돌하는 품질 속성 (Conflicting Attributes)**

* (Stage 1/2에서 식별된 충돌 NFR을 명시)  
* **속성 A (주요 목표):** 검색 성능 (NFR: P-01, 1초 내 응답)  
* **속성 B (희생 목표):** 데이터 일관성 (NFR: C-01, 즉각적 반영)

## **2\. 배경 및 문제 (Context)**

* (문제 상황. 예: 상품 정보 변경 시, 즉각적인 검색 인덱스 갱신은 DB 트랜잭션과 묶여 성능 저하 유발.)

## **3\. 고려된 해결 옵션 (Options Considered)**

### **옵션 1: Strong Consistency (동기식 갱신)**

* **설명:** 상품 DB 변경 트랜잭션 내에서 검색 인덱스 갱신.  
* **장점:** 데이터 즉시 일관성 보장.  
* **단점:** API 응답 시간 5초 이상 소요, 성능(P-01) 요구사항 실패.

### **옵션 2: Eventual Consistency (비동기식 갱신)**

* **설명:** 상품 DB 변경 후, 이벤트(Kafka)를 발행. 별도 워커가 이벤트를 구독하여 검색 인덱스 갱신.  
* **장점:** API 응답 시간 100ms 미만, 성능(P-01) 요구사항 충족.  
* **단점:** 최대 30초간 데이터 불일치 발생, 일관성(C-01) 요구사항 일부 희생.

## **4\. 결정 (Decision)**

* **옵션 2 (Eventual Consistency)를 채택한다.**  
* **근거:** 검색 성능(P-01)이 비즈니스 핵심 지표이며, 30초 이내의 데이터 불일치는 수용 가능한 트레이드오프로 판단됨.

## **5\. 영향 및 파급 효과 (Consequences)**

* (결과. 예: API 응답 시간 대폭 개선. 단, 프론트엔드 팀은 "정보가 갱신 중입니다"와 같은 UX를 고려해야 함.)

#### **2.1.3 ③ 기술 스택 ADR 템플릿 (Technology Stack)**

* **목적:** 특정 기술, 프레임워크, 라이브러리 선택을 비교/기록.  
* **파일 예시:** docs/adr/ADR-301\_Use\_FastAPI\_for\_API\_Server.md

# **ADR-301: \[기술 선택 제목\] (예: API 서버로 FastAPI 채택)**

상태: Accepted  
날짜: YYYY-MM-DD  
카테고리: 3\. 기술 스택 (Technology Stack)

## **1\. 배경 및 문제 (Context)**

* (우리가 해결하려는 문제. 예: 실시간성이 중요한 B-C-A 패밀리(Stage 1)의 API 서버가 필요하며, Python 생태계를 활용해야 함.)

## **2\. 요구사항 (Requirements)**

* (기술 선택의 기준. 예: R-01: 비동기(Async) 지원, R-02: 높은 성능, R-03: 자동 API 문서(Swagger) 생성, R-04: 빠른 개발 속도)

## **3\. 대안 비교 (Alternatives Comparison)**

| 기준 | 옵션 1: FastAPI | 옵션 2: Django (DRF) | 옵션 3: Flask |
| :---- | :---- | :---- | :---- |
| R-01: 비동기 지원 | ✅ (Native, ASGI) | 🔺 (부분적, ASGI) | 🔺 (부분적, ASGI) |
| R-02: 높은 성능 | ⭐️ (매우 높음) | ↓ (낮음) | ↓ (낮음) |
| R-03: API 문서 | ✅ (자동, Pydantic) | ↓ (수동/추가 작업) | ↓ (수동/추가 작업) |
| R-04: 개발 속도 | ✅ (매우 빠름) | ↓ (느림, 무거움) | ✅ (빠름) |

## **4\. 결정 (Decision)**

* **FastAPI를 API 서버 기술 스택으로 채택한다.**  
* **근거:** 모든 요구사항(R-01\~R-04)을 가장 높은 수준으로 만족하며, 특히 비동기 네이티브 지원과 Pydantic 기반 자동 문서화가 DNA Stage 1의 B-C-A 패밀리 목표와 일치함.

## **5\. 영향 및 파급 효과 (Consequences)**

* **긍정적:** 높은 성능, 타입 강제(Pydantic)로 인한 안정성 증가.  
* **부정적:** Flask/Django 대비 커뮤니티가 작음, ASGI 서버(Uvicorn)에 대한 학습 필요.

#### **2.1.4 ④ 도메인 기술 ADR 템플릿 (Domain Technology)**

* **목적:** 02G-00의 1 예시처럼, 'Priority Queue Scheduler'와 같은 비즈니스 로직을 해결하기 위한 *자체 개발* 아키텍처 패턴 결정.  
* **파일 예시:** docs/adr/ADR-401\_Priority\_Queue\_Scheduler\_Design.md

# **ADR-401: \[도메인 설계 제목\] (예: 주식 거래를 위한 우선순위 큐 스케줄러 설계)**

상태: Accepted  
날짜: YYYY-MM-DD  
카테고리: 4\. 도메인 기술 (Domain Technology)

## **1\. 배경 및 문제 (Context)**

* (비즈니스 문제. 예: 02G-00의 주식 거래 시스템은 초당 20건의 API Rate Limit이 있으나, 100개의 조건을 감시해야 함. 모든 조건을 5초마다 폴링하면(100/20) 중요 조건이 지연됨.)

## **2\. 결정 (Decision)**

* **'가중치 기반 우선순위 큐 스케줄러 (Weighted Priority Queue Scheduler)'를 자체 설계하여 도입한다.**  
* **핵심 로직:** 모든 조건(100개)을 큐에 넣되, '중요 조건'(예: 거래 임박)에 높은 가중치를 부여하여 API 호출(초당 20건)을 우선 할당한다.

## **3\. 아키텍처 설계 스케치 (Architecture Sketch)**

* (간단한 아키텍처 다이어그램이나 인터페이스 정의)  
* \-\> \-\> \-\>python

# **인터페이스 정의 (의사 코드)**

class PriorityScheduler:  
def add\_condition(condition\_id: str, priority: int):...  
def get\_next\_task() \-\> ConditionTask:...

\#\# 4\. 고려된 대안 (Alternatives Considered)  
\*   \*\*대안 1: 단순 Round Robin 폴링\*\*  
    \*   \*\*기각 사유:\*\* 5초 지연으로 인해 '정확성 A' NFR (Stage 1\) 위반.  
\*   \*\*대안 2: 외부 솔루션 구매\*\*  
    \*   \*\*기각 사유:\*\* 비용 문제(Stage 2 제약) 및 API 제약 환경에 맞는 솔루션 부재.

\#\# 5\. 영향 및 파급 효과 (Consequences)  
\*   (결과: Rate Limit 내에서 '중요' 조건은 1초 미만, '비중요' 조건은 10초 주기로 감시 가능. 시스템 복잡도 증가.)

#### **2.1.5 ⑤ DNA 시스템 ADR 템플릿 (DNA System)**

* **목적:** Stage 4 (04G-00) 1의 11개 DNA 시스템(로깅, 에러 등)에 대한 전사적 표준 결정.  
* **파일 예시:** docs/adr/dna-systems/ADR-001\_Structured\_Logging.md

# **ADR-001: (예: 구조화된 로깅을 위한 Structlog 채택)**

상태: Accepted  
날짜: YYYY-MM-DD  
카테고리: 5\. DNA 시스템 (DNA System)

## **1\. 배경 및 문제 (Context)**

* (DNA 시스템의 필요성. 예: 서비스가 분산됨에 따라 print()나 기본 logging 모듈로는 로그 추적(Traceability)이 불가능함. ELK 스택으로 로그를 수집해야 함.)

## **2\. 요구사항 (Requirements)**

* (R-01: 모든 로그는 JSON 형식이어야 함.)  
* (R-02: Request ID, User ID 등 Context 자동 주입이 가능해야 함.)  
* (R-03: Python 표준 logging과 호환되어야 함.)

## **3\. 대안 비교 (Alternatives Comparison)**

* **옵션 1: Structlog**  
  * **장점:** R-01, R-02, R-03 모두 충족, 유연한 설정.  
* **옵션 2: 표준 logging \+ JSONFormatter**  
  * **장점:** 표준 라이브러리.  
  * **단점:** R-02 (Context 주입)가 매우 복잡함.

## **4\. 결정 (Decision)**

* **모든 Python 기반 DNA 시스템의 로깅 표준으로 Structlog 라이브러리를 채택한다.**

## **5\. 영향 및 파급 효과 (Consequences)**

* (결과: 모든 로그가 일관된 JSON 형식으로 생성되어 ELK에서 분석 용이. 개발자들이 Structlog 사용법을 새로 학습해야 함.)

## **6\. 실행 및 강제화 (Enforcement)**

* (이 ADR이 어떻게 '표준'으로 연결되는지 명시)  
* **연결된 표준:** 06D-01\_Project\_Standards.md의 Mandatory-01: Logging 표준으로 이관됨.  
* **자동화:** ArchUnit/Fitness Function (Part 2.3.2 참조) 및 pre-commit 훅을 통해 import logging이나 print() 사용을 금지함.

### **2.2 Project Standards 템플릿 (DNA Stage 6 / 06D-01 형식)**

* **목적:** 귀사의 06G-00\_project\_standards\_guide.md 1에서 정의한 템플릿을 실제 마크다운으로 구현합니다. 이는 ADR의 *결정*을 실행 가능한 *규칙*으로 변환한 문서입니다.

# **06D-01: \[Project Name\] 프로젝트 표준**

버전: 1.0  
최종 수정: YYYY-MM-DD  
ADR 참조: ADR-001, ADR-002, ADR-003, ADR-301

---

## **Part 1: Mandatory 5 Standards (필수 5대 표준)**

(DNA Stage 6 1에서 정의한 5가지 필수 표준)

### **1.1 표준 01: 로깅 (Logging)**

* **ADR 근거**: ADR-001\_Structured\_Logging  
* **규칙**: 모든 어플리케이션 로그는 structlog를 통해 구조화된 JSON으로 생성해야 합니다.

| ✅ DO (해야 할 일) | ❌ DON'T (하지 말아야 할 일) |
| :---- | :---- |
| from structlog import get\_logger | import logging |
| logger \= get\_logger(\_\_name\_\_) | logger \= logging.getLogger(\_\_name\_\_) |
| logger.info("user\_login", user\_id=123) | logger.info(f"User {uid} logged in") |
| logger.error("error\_event", exc\_info=True) | print(f"Error: {e}") |

* **강제화 (Enforcement)**:  
  * **정적 (Static):** pre-commit 훅에서 print(), import logging 패턴 감지 시 커밋 실패. (Part 2.3.1 참조)  
  * **아키텍처 (Arch):** import-linter (Python) 또는 ArchUnit (Java)을 사용하여 logging 모듈 임포트 차단. (Part 2.3.2 참조)

### **1.2 표준 02: 에러 핸들링 (Error Handling)**

* **ADR 근거**: ADR-002\_Error\_Handling\_Standard  
* **규칙**: 모든 비즈니스 예외는 BaseProjectException을 상속하고, API는 표준화된 에러 포맷(e.g., RFC 7807)을 반환합니다.

| ✅ DO (해야 할 일) | ❌ DON'T (하지 말아야 할 일) |
| :---- | :---- |
| class UserNotFound(BaseProjectException):... | raise Exception("User not found") |
| except MyException as e: logger.error(...) | except Exception as e: pass (예외 무시) |
| raise HTTPException(404, detail="User not found") | raise Exception("Generic error") (컨텍스트 없음) |

* **강제화 (Enforcement)**:  
  * **정적:** 린트 룰(Ruff, ESLint)을 통해 except Exception: 또는 except: 사용 감지.  
  * **아키텍처:** ArchUnit/import-linter를 통해 Exception 클래스를 직접 raise 하는 코드 차단.

---

## **(기타 Mandatory 표준 3: 설정 관리, 4: 타입 힌트, 5: 테스트...)**

## **Part 2: Optional Standards (선택적 표준)**

(프로젝트 특화 표준)

### **2.1 표준 06: API 설계 (API Design)**

* **ADR 근거**: ADR-301\_Use\_FastAPI, ADR-305\_RESTful\_API  
* **규칙**: 모든 API는 RESTful 원칙을 따르며, Pydantic 모델을 사용해 요청/응답을 정의합니다.

| ✅ DO (해야 할 일) | ❌ DON'T (하지 말아야 할 일) |
| :---- | :---- |
| GET /v1/users/{user\_id} (경로) | GET /v1/getUser?id={user\_id} |
| class UserResponse(BaseModel):... (응답 모델) | return {"id": 1, "name": "test"} (Raw Dict) |
| 404 Not Found 반환 | 200 OK \+ {"error": "not found"} 반환 |
| POST /v1/users (자원 생성) | POST /v1/createUser (동사 사용) |

* **강제화 (Enforcement)**:  
  * **정적:** (어려움. 주로 코드 리뷰)  
  * **동적 (Fitness Function):** API 응답 시간을 체크하는 pytest 테스트 (Part 2.3.3 참조).

### **2.3 자동화 구현 예시 코드**

#### **2.3.1 ①.pre-commit-config.yaml 예시 (Python 프로젝트용)**

* **목적:** DNA 표준 01 (로깅)의 "DON'T" 항목 (print, import logging)을 커밋 시점에 자동 차단합니다.

YAML

\#.pre-commit-config.yaml  
\# (pre-commit install 을 통해 설치)

repos:  
\-   repo: https://github.com/pre-commit/pre-commit-hooks  
    rev: v4.6.0  
    hooks:  
    \-   id: check-yaml  
    \-   id: end-of-file-fixer  
    \-   id: trailing-whitespace

\# Ruff: Python Linter 및 Formatter (매우 빠름)  
\-   repo: https://github.com/astral-sh/ruff-pre-commit  
    rev: v0.4.4  
    hooks:  
    \-   id: ruff  
        args: \[\--fix\] \# 린트 오류 자동 수정  
    \-   id: ruff-format \# Black/isort 대체

\# DNA v4.0 표준 강제화 훅 (로컬)  
\-   repo: local  
    hooks:  
    \# 표준 01: print() 사용 금지  
    \-   id: disallow-print  
        name: "Disallow print()"  
        entry: "print\\\\(" \# 정규식  
        language: pygrep  
        types: \[python\]  
        description: "Prohibited: 'print()'. Use 'logger' from DNA System 01 (Logging)."

    \# 표준 01: 표준 logging 모듈 임포트 금지  
    \-   id: disallow-stdlib-logging  
        name: "Disallow standard 'logging' module"  
        entry: "^import logging" \# 정규식 (라인 시작)  
        language: pygrep  
        types: \[python\]  
        description: "Prohibited: 'import logging'. Use 'structlog' from DNA System 01 (Logging)."

#### **2.3.2 ② ArchUnit 테스트 예시 (Java, 순환 의존성 및 아키텍처 규칙 체크)**

* **목적:** DNA 표준 10 (아키텍처)의 "DO" 항목 (e.g., Domain은 Infrastructure에 의존 금지)을 자동 검증합니다. (Java의 ArchUnit 예시. Python은 import-linter가 유사한 역할 수행)

Java

// build.gradle (의존성 추가)  
// testImplementation 'com.tngtech.archunit:archunit-junit5:1.3.0'

// src/test/java/com/myproject/ArchitectureTest.java

import com.tngtech.archunit.core.domain.JavaClasses;  
import com.tngtech.archunit.core.importer.ClassFileImporter;  
import com.tngtech.archunit.lang.ArchRule;  
import org.junit.jupiter.api.Test;

import static com.tngtech.archunit.lang.syntax.ArchRuleDefinition.noClasses;  
import static com.tngtech.archunit.library.Architectures.layeredArchitecture;  
import static com.tngtech.archunit.library.GeneralCodingRules.NO\_CLASSES\_SHOULD\_HAVE\_CYCLES;

class ArchitectureTest {

    // 테스트할 클래스 로드  
    private final JavaClasses importedClasses \= new ClassFileImporter()  
       .importPackages("com.myproject");

    /\*\*  
     \* \[Fitness Function\]  
     \* DNA 표준: 순환 의존성 금지 (모든 시스템)  
     \*/  
    @Test  
    void no\_cyclic\_dependencies() {  
        ArchRule myRule \= NO\_CLASSES\_SHOULD\_HAVE\_CYCLES;  
        myRule.check(importedClasses);  
    }

    /\*\*  
     \* \[Fitness Function\]  
     \* DNA 표준 10: Layered Architecture 강제 (Domain은 Infra 의존 불가)  
     \*/  
    @Test  
    void layered\_architecture\_is\_respected() {  
        ArchRule myRule \= layeredArchitecture()  
           .consideringAllDependencies()  
            // 1\. Layer 정의  
           .layer("Domain").definedBy("..domain..")  
           .layer("Application").definedBy("..application..")  
           .layer("Infrastructure").definedBy("..infrastructure..")  
           .layer("Presentation").definedBy("..presentation..")  
              
            // 2\. Layer 간 규칙 정의  
           .whereLayer("Presentation").mayOnlyAccessLayers("Application")  
           .whereLayer("Application").mayOnlyAccessLayers("Domain", "Infrastructure")  
           .whereLayer("Infrastructure").mayOnlyAccessLayers("Domain")  
            // ⭐️ Domain은 그 누구에게도 의존하지 않음 (Java 표준 라이브러리 제외)  
           .whereLayer("Domain").shouldNotDependOnAnyLayer(); 

        myRule.check(importedClasses);  
    }  
}

#### **2.3.3 ③ Fitness Function pytest 예시 (Python, API 응답시간 NFR 체크)**

* **목적:** DNA 표준 06 (API)의 "DO" 항목 (e.g., API 응답은 200ms 이내)을 CI/CD 파이프라인에서 지속적으로 검증합니다.

Python

\# requirements-test.txt (의존성 추가)  
\# pytest  
\# requests  
\# numpy (P95 계산용)

\# tests/fitness/test\_nfr\_performance.py  
import pytest  
import requests  
import time  
import numpy as np

\# NFR (Non-Functional Requirement) 정의  
\# DNA Stage 1/2 에서 도출된 값  
NFR\_P95\_RESPONSE\_TIME\_LIMIT\_MS \= 200.0   
API\_ENDPOINT \= "https://api.myproject.com/v1/health" \# 검증할 API 엔드포인트

@pytest.mark.fitness \# 'fitness' 마커로 일반 테스트와 분리  
def test\_api\_health\_check\_response\_time\_p95():  
    """  
     
    헬스체크 API의 P95 응답 시간이 NFR 임계값(200ms)을 초과하는지 확인  
    """  
    response\_times\_ms \=  
    sample\_size \= 50 \# P95 측정을 위한 샘플 크기

    print(f"\\n {API\_ENDPOINT} NFR 검증 (P95 \< {NFR\_P95\_RESPONSE\_TIME\_LIMIT\_MS} ms)...")

    for i in range(sample\_size):  
        try:  
            start\_time \= time.perf\_counter()  
            response \= requests.get(API\_ENDPOINT, timeout=1.0)  
            end\_time \= time.perf\_counter()  
              
            response.raise\_for\_status() \# 2xx가 아니면 에러  
              
            duration\_ms \= (end\_time \- start\_time) \* 1000  
            response\_times\_ms.append(duration\_ms)  
            time.sleep(0.1) \# 간단한 쿨다운  
        except requests.RequestException as e:  
            pytest.fail(f"API 요청 실패 (샘플 {i+1}/{sample\_size}): {e}")

    if len(response\_times\_ms) \< sample\_size:  
        pytest.fail("NFR 검증을 위한 샘플 크기를 채우지 못했습니다.")

    \# P95 계산 (Numpy 사용)  
    p95\_response\_time \= np.percentile(response\_times\_ms, 95)

    print(f" P95 응답 시간: {p95\_response\_time:.2f} ms")

    \# NFR 검증 (Fitness Function)  
    assert p95\_response\_time \<= NFR\_P95\_RESPONSE\_TIME\_LIMIT\_MS, \\  
        f"P95 응답 시간({p95\_response\_time:.2f}ms)이 NFR 임계값({NFR\_P95\_RESPONSE\_TIME\_LIMIT\_MS}ms)을 초과했습니다."

\# CI/CD 설정 (e.g.,.github/workflows/main.yml)  
\# \- name: Run Fitness Functions (on Staging)  
\#   run: pytest \-m fitness \# 'fitness' 마커가 붙은 테스트만 Staging 환경에 실행

---

## **Part 3: Comparative Methodology Verification & Gap Analysis (Category 3\)**

이 섹션에서는 귀사의 DNA v4.0 방법론 1을 SEI ADD와 같은 기성 프레임워크와 비교하고, AI 협업과 같은 미래 요구사항에 대한 격차를 분석하여 외부 검증을 제공합니다.

### **3.1 Comparative Analysis: SEI Attribute-Driven Design (ADD) vs. DNA v4.0**

#### **3.1.1 핵심 개념 비교**

* **SEI ADD (Attribute-Driven Design):** 1990년대 카네기 멜론 대학 Software Engineering Institute(SEI)에서 개발한 *아키텍처 설계 방법론*입니다. '품질 속성(Quality Attributes)' (NFR)을 시스템 설계의 가장 중요한 동인(Driver)으로 보고, 이를 만족시키기 위한 아키텍처 전략과 전술을 체계적으로 도출하는 데 중점을 둡니다.  
* DNA v4.0 1: 2020년대 엔터프라이즈 환경을 위한 *엔드-투-엔드 개발 라이프사이클*입니다. SEI ADD의 핵심 사상('품질 속성' 중심 설계)을 Stage 2에 1 *전략적으로 내장*하고 있으며, 이를 넘어서 자동화된 거버넌스 및 구현(Stage 4-9) 1까지 확장한 포괄적인 프레임워크입니다.

#### **3.1.2 프로세스 매핑 비교표**

| SEI ADD v2.0 (단계) | DNA v4.0 | 분석 및 차이점 |
| :---- | :---- | :---- |
| **Step 1:** 시스템 분해 (Choose element) | **DNA Stage 7:** Blueprint (07G-00) | ADD는 이미 분해할 요소를 안다고 가정하고 시작합니다. DNA는 Stage 1-6을 통해 *무엇을* 분해할지 체계적으로 결정합니다. |
| **Step 2:** 품질 속성 식별 (Identify Drivers) | **DNA Stage 1:** NFR Profile (01G-00) | **(일치)** 두 방법론 모두 품질 속성(NFR)을 아키텍처의 핵심 동인으로 봅니다. |
| **Step 3:** 아키텍처 개념 선택 (Choose concepts) | **DNA Stage 1:** 7 Families (01G-00) | **(DNA 독창성 \#1)** ADD는 이 과정을 "아키텍트의 경험과 직관"에 의존합니다. DNA는 이 과정을 '7개 패밀리'라는 체계적인 휴리스틱으로 *표준화*하고 *가속화*했습니다. |
| **Step 4:** 개념 인스턴스화 및 인터페이스 정의**Step 5:** 설계 검증 및 구체화 | **DNA Stage 2:** 5단계 구현 프로세스 (02G-00)(Function → Attribute → Constraint → Technology → Architecture) | **(사실상 동일)** 귀사의 Stage 2 프로세스는 SEI ADD의 핵심 설계 루프(Loop)를 DNA v4.0에 맞게 *완벽하게 구현*한 것입니다. |
| (ADD에 없음) | **DNA Stage 3:** ADR 작성 (03G-00) | **(DNA 독창성 \#2)** ADD는 '설계'에 집중하지만, DNA는 '설계 결정'을 5개 카테고리로 *명시적*으로 기록하고, 이를 Stage 6/8/9의 거버넌스와 구현의 *입력*으로 사용합니다. |
| (ADD에 없음) | **DNA Stage 4-5:** 11 DNA System (04G-00, 05G-00) | **(DNA 독창성 \#3)** ADD는 로깅, 에러 처리와 같은 공통(Cross-cutting) 기능을 명시적으로 다루지 않습니다. DNA는 이를 '11개 DNA 시스템'으로 *표준화*하고 *사전 구축*하여 개발 생산성을 극대화합니다. |
| (ADD에 없음) | **DNA Stage 6, 8, 9:** Governance & Automation (06G-00, 08G-00, 09G-00) | **(DNA 독창성 \#4)** ADD는 '설계'에서 끝납니다. DNA는 설계를 'Standards', 'Tasks', 'Checklists'로 변환하여, *자동화된 구현과 지속적인 검증*(Fitness Functions)으로 연결합니다. |

#### **3.1.3 DNA v4.0의 독창성 및 방향성 검증**

SEI ADD와의 비교를 통해 DNA v4.0은 매우 올바른 방향성을 가지며, 다음과 같은 4가지 핵심적인 독창성을 지니고 있음이 검증되었습니다:

1. **경험의 체계화:** SEI ADD가 "아키텍트의 직관"에 의존하는 아키텍처 패턴 선택(Step 3)을, DNA v4.0은 '7개 패밀리' (01G-00) 1라는 데이터 기반 휴리스틱으로 *체계화*했습니다. 이는 아키텍트의 개인 역량에 대한 의존도를 낮춥니다.  
2. **설계와 거버넌스의 통합:** SEI ADD는 '설계 방법'에 머무르지만, DNA v4.0은 Stage 3(ADR)과 1 Stage 6(Standards)를 1 통해 설계를 단순한 문서가 아닌, *지속적인 거버넌스* 메커니즘으로 통합했습니다.  
3. **구현의 가속화:** '11개 DNA 시스템' (04G-00) 1을 표준화하고 사전 구축함으로써, SEI ADD로 잘 '설계'된 시스템을 실제 '구현'하는 과정의 속도와 일관성을 극대화했습니다.  
4. **엔드-투-엔드 자동화:** DNA v4.0은 설계(Stage 1-2)에서 결정(Stage 3), 표준화(Stage 6), 구현(Stage 8-9) 1까지 이어지는 전 과정을 자동화(Fitness Functions, PaC)와 연결하려는 명확한 목표를 가지고 있습니다.

### **3.2 DNA v4.0에서 빠진 것 (Gap Analysis)**

#### **3.2.1 일반 엔터프라이즈 프로세스와 비교**

* **누락된 점 1: 운영(Operations) 및 SRE (Site Reliability Engineering):**  
  * **분석:** DNA v4.0 1은 Stage 9 (구현)에서 끝나는 것으로 보입니다. 실제 엔터프라이즈 라이프사이클은 '배포(Deploy)', '운영(Operate)', '모니터링(Monitor)', '장애 대응(Incident Response)' 단계를 포함합니다.  
  * **권장 사항:** Stage 1의 'NFR 프로파일' (01G-00) 1에 '운영성(Operability)'과 '복원성(Resilience)' (e.g., MTTR, MTBF) 지표를 명시적으로 추가해야 합니다. 또한 Stage 5 ('DNA 시스템 구현') 1에 모니터링, 알람, 추적(Tracing) 시스템(e.g., Prometheus, Grafana, OpenTelemetry)을 '11개 DNA 시스템'의 일부로 필수로 포함시켜야 합니다.  
* **누락된 점 2: 데이터 거버넌스 (Data Governance):**  
  * **분석:** '11개 DNA 시스템' (04G-00) 1이 주로 코드(Code)와 아키텍처에 중점을 두고 있습니다. 그러나 데이터의 생명주기(생성, 저장, 접근, 마스킹, 폐기)에 대한 거버넌스가 상대적으로 부족합니다.  
  * **권장 사항:** 'DNA 시스템 10: Data System'을 확장하여, 데이터 스키마 버전 관리(e.g., Alembic, Flyway), 마이그레이션 정책, PII(개인 식별 정보) 마스킹 표준, 데이터 접근 제어 정책을 DNA 표준(06D-01)에 1 명시적으로 포함해야 합니다.

#### **3.2.2 AI 협업 특화로 추가해야 할 것**

AI/LLM 기반 시스템은 기존 소프트웨어와 근본적으로 다릅니다. (1) 코드가 아닌 *모델*이 로직의 핵심이며, (2) 로직이 \*비결정적(Non-deterministic)\*이고, (3) *데이터*가 코드를 생성(학습)합니다.

* **추가 권장 사항:**  
  1. **Stage 1 (01G-00) NFR 프로파일 확장:**  
     * 기존 '정확성(Accuracy)' 외에, AI 특화 NFR인 '공정성(Fairness)', '설명 가능성(Explainability)', '재현성(Reproducibility)' NFR을 추가해야 합니다.  
  2. **Stage 3 (03G-00) 신규 ADR 카테고리 추가:**  
     * ADR Category 6: 모델 선택 (Model Selection): (e.g., GPT-4 vs. Llama 3, Fine-tuning vs. RAG 전략 비교)  
     * ADR Category 7: 데이터 출처 (Data Provenance): (e.g., 학습 데이터셋, 라이선스, 전처리 파이프라인)  
  3. **Stage 4/5 (04G-00, 05G-00) 'DNA 시스템' 확장:**  
     * DNA System 12: MLOps 파이프라인: 모델 학습, 버전 관리(e.g., MLflow, DVC), 배포(Serving) 표준화.  
     * DNA System 13: 프롬프트 관리 시스템: 프롬프트 템플릿, 버전 관리, A/B 테스트 표준화.  
     * DNA System 14: AI 안전성 (AI Safety): 입출력 가드레일, 민감정보 필터링, 할루시네이션(Hallucination) 대응 표준화.  
  4. **Stage 6 (06G-00) 'Project Standards' 확장:**  
     * Mandatory-06: 모델 검증 (Model Validation): 모델 성능(e.g., ROUGE, BLEU, F1-score) 및 편향성 테스트를 CI/CD 파이프라인(Fitness Function)에 통합.

### **3.3 Policy-as-Code vs. Architectural Fitness Functions**

#### **3.3.1 각각 언제 사용하는가?**

* **Policy-as-Code (PaC):**  
  * **개념:** *예방적(Preventive)* 거버넌스입니다. "무엇이 허용되는가?"를 정의합니다. 주로 \*선언적(Declarative)\*이며, 시스템의 정적 상태(인프라, 설정)를 검사합니다.  
  * **예시:** OPA (Open Policy Agent), Terraform Sentinel, AWS SCP.  
  * **사용 시점:** 인프라 배포 시(CI/CD), 설정 변경 시. (e.g., "S3 버킷은 Public Access를 금지한다.")  
* **Architectural Fitness Functions:**  
  * **개념:** *진화적(Evolutionary)* 거버넌스입니다. "시스템이 원하는 속성을 유지하는가?"를 검증합니다. 주로 \*코드 기반(Code-based)\*이며, 아키텍처, NFR, 코드 품질을 테스트합니다.  
  * **예시:** ArchUnit (Part 2.3.2), Pytest (Part 2.3.3), Netflix의 Chaos Monkey.  
  * **사용 시점:** CI/CD 파이프라인 실행 시, 주기적 헬스체크 시. (e.g., "Domain 레이어가 Infra 레이어를 호출하지 않는다.")

#### **3.3.2 ADR 5개 카테고리별 적용 및 우선순위**

이 두 가지는 상호 보완적입니다. **PaC**는 ADR의 *외부 제약*을 강제하고, **Fitness Function**은 ADR의 *내부 설계*를 강제합니다.

| DNA ADR 카테고리 | 적용 도구 | 적용 예시 및 우선순위 |
| :---- | :---- | :---- |
| **1\. 외부 제약 ADR** | **Policy-as-Code** (PaC) | **(우선순위: 매우 높음)**"AWS Seoul 리전만 사용" (ADR-101) → OPA/Sentinel 정책으로 provider "aws"의 region 속성을 강제합니다. |
| **2\. 충돌 해결 ADR** | **Fitness Function** | **(우선순위: 중간)**"성능을 위해 비동기 갱신" (ADR-201) → API 응답 시간을 측정하는 Pytest (Part 2.3.3)를 통해 성능 NFR이 유지되는지 지속 검증합니다. |
| **3\. 기술 스택 ADR** | **Policy-as-Code** | **(우선순위: 높음)**"FastAPI 사용" (ADR-301) → PaC를 사용해 requirements.txt 또는 pom.xml에 django나 spring-boot가 포함되는 것을 금지합니다. |
| **4\. 도메인 기술 ADR** | **Fitness Function** | **(우선순위: 높음)**"우선순위 큐 설계" (ADR-401) → 이 스케줄러가 실제로 우선순위가 높은 작업을 먼저 처리하는지 검증하는 단위/통합 테스트를 작성합니다. |
| **5\. DNA 시스템 ADR** | **Fitness Function** | **(우선순위: 매우 높음)**"Structlog 사용" (ADR-001) → ArchUnit (Part 2.3.2)을 사용해 java.logging 패키지를 사용하는 클래스가 있는지 검사하고 빌드를 실패시킵니다. |

---

## **Part 4: Step-by-Step Execution and Governance Guides (Category 4\)**

이 섹션은 DNA v4.0 방법론 1을 실행하기 위한 3가지 핵심 실천 가이드를 초보자도 따라 할 수 있도록 상세하게 제공합니다.

### **4.1 Guide 1: ADR 작성 step-by-step (외부 제약 ADR 예시)**

목표: Stage 2 (02G-00) 1에서 식별된 제약을 Stage 3 (03G-00) 1 ADR로 문서화하는 상세 절차.  
시나리오: Stage 2 분석 결과, "고객사(금융)의 보안 규정으로 인해 모든 데이터베이스 접근은 특정 IP 대역(10.x.x.x)에서만 허용되어야 함"이라는 외부 제약이 식별됨.

#### **Step 1: 무엇을 결정할 것인가 파악 (Trigger)**

1. **문제 식별:** 개발자가 자신의 로컬 PC(공인 IP)에서 프로덕션 DB에 직접 접근할 경우, 보안 규정을 위반하게 됩니다.  
2. **결정 대상:** "프로덕션 DB 접근 통제 정책 수립"  
3. **DNA 카테고리 식별:** "보안 규정"은 협상 불가능한 외부 요인이므로 02G-00 1에 따라 \*\*"1. 외부 제약 ADR"\*\*로 분류합니다.

#### **Step 2: 어떤 템플릿을 사용하는가 (Template Selection)**

* Part 2.1.1에서 제공한 ① 외부 제약 ADR 템플릿을 선택하고 파일(ADR-102\_Prod\_DB\_Access\_Policy.md)을 생성합니다.

#### **Step 3: 각 섹션을 어떻게 채우는가 (Drafting)**

* **1\. 제약 사항 (Constraint)**: "모든 프로덕션 데이터베이스(AWS RDS) 접근은 승인된 IP 대역(VPC 내부: 10.x.x.x)으로 제한되어야 한다."  
* **2\. 제약의 출처 (Source of Constraint)**: "고객사 XXX의 금융 보안 규정 3.1.a (데이터 접근 통제)"  
* **3\. 결정 (Decision)**:  
  1. "프로덕션 RDS의 Security Group Inbound Rule은 10.x.x.x/16 대역만 허용하도록 설정한다."  
  2. "개발자의 프로덕션 DB 접근은 10.x.x.x 대역 내에 위치한 'Bastion Host'를 통해서만 허용한다."  
* **4\. 대안 (Alternatives Considered)**: "대안 없음. (보안 규제 사항임)"  
* **5\. 영향 및 파급 효과 (Consequences)**:  
  * (긍정적) 보안 감사를 통과할 수 있으며, 데이터 접근 경로가 일원화됩니다.  
  * (부정적) 개발자의 DB 직접 접근이 차단됩니다. 모든 DB 작업(쿼리, 점검)은 Bastion Host를 경유해야 하므로 1단계의 오버헤드가 발생합니다.  
* **6\. 강제화 방안 (Enforcement)**:  
  * "Terraform을 사용하여 Security Group을 코드(PaC)로 관리하며, 수동 변경을 금지하고 변경 사항을 감시합니다."

#### **Step 4: 검토 기준은? (Review & Acceptance)**

* \[ \] **명확성:** 제약과 결정이 명확하고 구체적으로 기술되었는가?  
* \[ \] **출처:** 제약의 근거(법규, 계약)가 명시되었는가?  
* \[ \] **영향 분석:** 이 결정이 개발팀과 운영팀에 미칠 영향이 정직하게 분석되었는가? (e.g., Bastion Host 사용으로 인한 불편함)  
* \[ \] **강제화:** 이 결정이 지켜질 수 있는 자동화 방안(PaC)이 포함되었는가?

*위 기준을 통과하면 ADR 상태를 Proposed에서 Accepted로 변경합니다.*

### **4.2 Guide 2: ADR → Project Standards 변환 방법**

목표: Stage 3 (03G-00) 1의 '결정'을 Stage 6 (06G-00) 1의 '실행 규칙'으로 변환하는 프로세스.  
시나리오: ADR-001: Structlog 채택 (Part 2.1.5)을 06D-01 표준 문서(Part 2.2)로 변환합니다.

#### **Step 1: ADR의 '결정(Decision)'과 '강제화(Enforcement)' 섹션 식별**

* *ADR-001 Decision:* Structlog를 표준으로 채택한다.  
* *ADR-001 요구사항:* JSON 형식, Context 주입.  
* *ADR-001 Enforcement:* print() 금지, import logging 금지.

#### **Step 2: 06D-01 템플릿 (Part 2.2) 준비**

* 06D-01\_Project\_Standards.md 파일을 엽니다.  
* Part 1: Mandatory Standards 아래에 1.1 표준 01: 로깅 (Logging) 섹션을 생성합니다.

#### **Step 3: 'DO / DON'T' 규칙으로 재구성**

* ADR의 추상적인 '결정'을 개발자가 즉시 따를 수 있는 명확한 *실행 규칙*으로 번역합니다.

| ADR-001 (결정) | ➡️ | 06D-01 (규칙) |
| :---- | :---- | :---- |
| "Structlog를 사용한다" | ➡️ | ✅ DO: from structlog import get\_logger |
| "JSON 형식이어야 한다" | ➡️ | ✅ DO: logger.info("event", key="value") |
| (위 규칙 위반) | ➡️ | ❌ DON'T: logger.info(f"msg {var}") (JSON 형식이 깨짐) |
| "print()는 금지한다" | ➡️ | ❌ DON'T: print("message") |
| "import logging은 금지한다" | ➡️ | ❌ DON'T: import logging |

#### **Step 4: 'Enforcement' 섹션 구체화**

* ADR에서 '금지'된 항목을 *어떻게* 자동화할지 구체적인 코드와 도구를 명시합니다.  
* **ADR-001 (Enforcement):**  
  * "pre-commit 훅으로 print() 금지"  
* **06D-01 (Enforcement):**  
  * 강제화 (Enforcement):  
  * 정적 (Static): .pre-commit-config.yaml에 다음 pygrep 훅을 추가하여 print() 및 import logging 사용 시 커밋을 실패시킵니다. (코드 예시는 Part 2.3.1 참조)

YAML

\-   repo: local  
    hooks:  
    \-   id: disallow-print  
        entry: "print\\\\("  
       ...  
    \-   id: disallow-stdlib-logging  
        entry: "^import logging"  
       ...

### **4.3 Guide 3: Compliance 구현 순서 (자동화 성숙도 모델)**

목표: ADR과 표준을 자동화로 구현하는 현실적인 3단계 로드맵을 제시합니다.  
무엇부터 자동화하는가? \-\> 가장 비용이 적게 들고(Low Cost), 효과가 크며(High Impact), 피드백 루프가 빠른(Fast Feedback) 것부터 시작합니다.

#### **Phase 1: 최소한의 자동화 (정적 분석) \- "Day 1"**

* **목표:** 코드 스타일 일관성 확보 및 명백한 오류 방지.  
* **도구:** pre-commit 훅, Ruff/ESLint (Linter), Black/Prettier (Formatter).  
* **적용 대상 표준:**  
  * 표준 01 (로깅): print() 금지 (Part 2.3.1).  
  * DNA v4.0의 모든 코드 포맷팅 표준.  
* **이유:** 개발자 PC에서 커밋 시점에 즉각적인 피드백을 줍니다. CI 리소스를 절약하고 가장 기본적인 일관성을 확보합니다. (최소 비용, 최대 효과)

#### **Phase 2: 아키텍처 자동화 (Fitness Functions) \- "Week 2"**

* **목표:** DNA 시스템 및 아키텍처 규칙(Layer) 강제.  
* **도구:** ArchUnit (Java, Part 2.3.2), import-linter (Python).  
* **적용 대상 표준:**  
  * 표준 01 (로깅): import logging 금지.  
  * 표준 10 (아키텍처): domain \-\> infrastructure 의존성 금지.  
  * 표준 02 (에러): Exception 직접 raise 금지.  
* **이유:** CI/CD 파이프라인에서 아키텍처가 손상되는 것을 방지합니다. 아키텍처는 시간이 지남에 따라 엔트로피가 증가하기 때문에 초기에 자동화하는 것이 중요합니다.

#### **Phase 3: NFR 및 인프라 자동화 (PaC & 동적 분석) \- "Month 1+"**

* **목표:** NFR(성능, 보안) 및 외부 제약(인프라) 강제.  
* **도구:** pytest (Part 2.3.3), OPA / Terraform Sentinel (Policy-as-Code).  
* **적용 대상 표준:**  
  * 표준 (외부 제약): "AWS Seoul 리전만 허용" (PaC로 강제). (Part 4.1 예시)  
  * 표준 (API): "P95 응답 시간 200ms 미만" (Pytest Fitness Function으로 검증).  
* **이유:** 가장 강력한 거버넌스 형태이지만, 설정과 유지보수에 비용이 듭니다. 시스템이 안정화된 후 NFR과 보안을 지속적으로 검증하기 위해 도입합니다.

#### **참고 자료**

1. DNA\_Methodology.txt